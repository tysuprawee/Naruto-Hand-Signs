# Copy this file to .env.release before Windows packaging.
# Keep only client-safe values here.

SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
DISCORD_CLIENT_ID=your_discord_client_id_here

# Optional if your game still reads this fallback key path in some flows.
# Keep it equal to anon key only.
# SUPABASE_ANON_KEY=your_anon_key_here

# NEVER put service role keys in release files:
# SUPABASE_SERVICE_ROLE_KEY=...
